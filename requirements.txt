# Cross-Domain Recommendation — pinned environment.
# Covers data acquisition + Data Understanding (CRISP-DM §2.x) EDA.
# Modeling deps (torch, sentence-transformers, scikit-learn, wandb) will be
# added in a separate requirements file when we reach §S3 / §S4 of the project plan.

# data
pandas==2.2.2
numpy==1.26.4
pyarrow==16.1.0

# plotting
matplotlib==3.9.2
seaborn==0.13.2

# progress + ingestion
tqdm==4.66.5
huggingface_hub==0.36.0

# text embeddings (§3.3 — MiniLM via sentence-transformers; torch is a transitive dep)
sentence-transformers==3.0.1

# notebook execution
jupyter==1.0.0
ipykernel==6.28.0
nbformat==5.10.4
nbconvert==7.16.4
